export default function EmailConfirmationSent() {
    return (
        <body className="container">
            <h1>
                Email confirmation sent to your email address, verify your email
                address and proceed to login
            </h1>
        </body>
    );
}
